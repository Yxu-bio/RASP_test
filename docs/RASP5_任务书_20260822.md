# RASP5 项目任务书

版本：2026-08-22
状态：当前权威路线
最近状态更新：2026-08-28（WP2 clade/node identity 审计完成）
替代文档：`RASP5_任务书_20260712.md`
适用范围：`E:/RASP` Python 桌面版 RASP5 主项目，不包含 Rust 重写 BioGeoBEARS 的独立研究支线。

## 0. 执行摘要

RASP5 已经完成从“旧版 RASP 功能复刻”到“历史生物地理与祖先性状桌面工作台”的主体建设。核心祖先分布方法、树集合方法、性状重建入口、空间数据编码、BioGeoBEARS BSM 事件和网络可视化均已有真实实现。当前主要矛盾不再是功能缺失，而是：

1. 已形成可回溯的 M0.5 checkpoint（`rasp5-m0.5-20260827`），后续改动以该标签为回归基线；
2. 核心方法的 Psychotria 数值金标准、参数传播、S 系列分母及跨 topology clade/node identity 已固化，WP2 剩余重点是 BBM 旧案例对照；
3. Phase 2 已形成独立 BSM/空间契约门禁；Dore 衍生的 RASP adjacency-path 1000-map 已固定为可重建规范，但正式 1000-map 数值输出和论文等价性尚未验证；
4. 性状展示和大树可视化仍有实验性范围漂移；Lineage Range Dynamics 已封锁公开入口并移出 M1；
5. README、项目许可证、CI、installer 和干净环境验收尚未完成。

当前位置定义为 **M0.5 Integration and evidence checkpoint + WP1 complete + WP2 in progress**。下一目标仍是 **M1 Feature-complete Beta**。WP2 已完成 lagrange-ng/BayArea 参数传播、Psychotria 跨模型金标准、S 系列分母和 clade/node identity 审计，当前唯一未完成的 WP2 核心项是 BBM 旧案例对照。

| 工作面 | 当前判断 | M1 前必须完成 |
| --- | --- | --- |
| 祖先分布重建 | 主链完整，多数为 Stable candidate | BBM 旧案例对照及 WP2 收尾 |
| 性状重建 | 入口较完整，Beta/Experimental 混合 | 冻结模型边界，明确哪些输出能画节点状态 |
| 空间数据 | occurrence 到 range matrix 主链已成形 | 固化 schema、真实大数据回归、Region Builder 收口 |
| BSM 事件与网络 | 事件表、网络编辑和独立 Phase 2 门禁可用 | 正式重跑 1000 maps，并在 WP5 核对作者 allowed-state 路径和论文统计 |
| 大树与结果视图 | 已有专项优化，仍在工作区开发中 | 固定 fixture、完成 1534-tip GUI/导出回归 |
| 发布工程 | 有独立引擎包结构，manifest audit 已通过 | 文档许可、CI、installer、clean install |

当前总原则：**首个正式版本不再扩展新的顶层分析方法，先把现有方法变成可验证、可安装、可引用和可复现的软件。**

```text
M0.5 当前集成现场
  -> M1 Feature-complete Beta
       范围冻结 + 干净 checkpoint + 核心数值可信 + 自动检查真实覆盖
  -> M2 Release Candidate
       安装包 + 文档许可 + clean Windows 验收 + 外部用户测试
  -> M3 Manuscript-ready
       固定 release + benchmark + 论文证据 + 第三方复现
  -> M4 Advanced research
       新算法、新生物学问题和延期研究功能
```

## 1. 项目开发历程与关键决策

### 1.1 从界面复刻转向算法工作流复刻

项目最初目标是复刻旧版 RASP 的全部功能，但很快确认：真正需要复刻的是输入、配置、引擎调用、统计语义和输出工作流，而不是把 VB.NET 逐行翻译为 Python。

因此形成了当前基本架构：

```text
GUI
  -> config model
  -> analysis service
  -> external engine runner
  -> raw output parser
  -> normalized result
  -> tree/table/map renderer and exporter
```

Python 不重新实现 DIVA、DEC、BioGeoBEARS、BayArea、MrBayes 或 BayesTraits 的核心算法，只负责可靠编排和展示。

### 1.2 S-DIVA 的第一次核心纠偏

早期 Python 版逐棵调用 DIVA 后自行汇总，结果与旧版 RASP 明显不一致。调查旧源码后确认，旧版 S-DIVA 使用修改版 `DIVA.exe` 的 config/proc 工作流完成统计。

已完成：

- 改为由修改版 DIVA 引擎承担 S-DIVA 统计；
- 对齐随机树抽样、Final tree、Fossil、Include/Exclude、Threads；
- 对齐默认 keep、Random Step、Save/Load Setting；
- 修复卡死、Fossil 约束和节点映射问题。

这一阶段确立了后续原则：**先确认原版真实逻辑，再决定对齐旧版还是采用新引擎的合理语义。**

### 1.3 DEC/S-DEC 转向 lagrange-ng 原生语义

旧版 RASP 使用的修改版 Lagrange 支持 `includedists/excludedists`，当前项目使用 `lagrange-ng.exe`。两者不是简单语法替换。

已完成：

- DEC 与 S-DEC 使用 lagrange-ng 原生配置；
- 支持 Maximum areas、period area rules、dispersal constraints、Fossil/MRCA；
- 修复 root age/tree scaling；
- S-DEC 使用外层 Threads 并行多棵树，每个子进程固定 OpenBLAS 单线程；
- 移除旧 Lagrange 兼容执行分支，不再假装 post-parse 过滤等价于旧算法。

### 1.4 lagrange-ng 上游问题修复

实测发现上游源码在 `evaluate` 模式中读入固定 dispersion/extinction 后，计算路径仍可能使用默认 `0.01`；period d/e parser/fallback 也不完整。

已完成：

- 查明源码数据流；
- 修复固定 d/e 和 period d/e 接线；
- 重新编译 Windows 引擎；
- 保留补丁和使用说明；
- 当前 RASP 已接入修复版引擎。

### 1.5 BioGeoBEARS 主链与 BSM 分离

已完成 BGB、S-BGB 和六模型检测：

- DEC、DEC+J、DIVALIKE、DIVALIKE+J、BAYAREALIKE、BAYAREALIKE+J；
- null range 与 DEC* 的合理处理；
- +J 模型先运行对应无 +J 模型，再传递 d/e 初值；
- batch runner 减少重复加载 R/BioGeoBEARS；
- S-BGB 使用旧版 clade 完全匹配式合并；
- model test 输出 AIC、AICc、weights、LRT/teststable。

BSM 曾被错误地嵌入普通结果树并覆盖树图。随后已明确分离：

- 普通 BGB 结果只展示节点祖先状态；
- BSM 由独立入口生成或载入；
- BSM 事件表和网络分析使用随机映射事件，不污染普通结果树。

### 1.6 BayArea 与 BBM

BayArea 已从旧版未知 DLL 转为重新编译的 BayArea 1.0.3 exe runner，并实现坐标、MCMC、Tracer、burn-in、seed、多链和原始结果保存。期间修复了 `distancePowerPrior` 表面传入但计算硬编码的问题，以及相关初始化/迭代器问题。

当前结论：

- `DISTANCE_NORM` 为 Beta/release candidate；
- `INDEPENDENCE` 仅保留兼容入口，并明确不推荐；
- 仍需独立链收敛基准和长链用户教程。

BBM 已通过 MrBayes 接入，支持节点选择、OG0 advanced、进度/取消、结果解析和树图。旧 `BAYESDLL.dll` 不再作为默认主链。

### 1.7 性状重建扩展

当前 Trait Reconstruction 已包含：

- BayesTraits V5 MultiState；
- Continuous Model A/B；
- Independent Contrast、Correlation、Fat Tail 等 runner；
- phytools fastAnc、fastAnc CI、anc.Bayes、anc.ML；
- ape ace ER/SYM/ARD；
- S-phytools、S-ape；
- log/log10 分析、back-transform 和 contMap-like 展示。

这一阶段也出现过范围漂移：DTT、论文 A/B/C 图、regime/group 和多树连续性状估计一度混在 BayesTraits 配置与结果中。当前任务不是继续加模型，而是明确“计算引擎输出”和“展示/二次分析”之间的边界。

### 1.8 空间数据与 BSM 网络

已完成的空间数据主链：

```text
occurrence / GeoJSON / taxon table
  -> taxon matching
  -> point-in-polygon
  -> area assignment audit
  -> binary range matrix
  -> ancestral reconstruction
```

已实现 Spatial Data Manager、XLSX/CSV、混合性状列选择、149k occurrence 编码、Region GeoJSON Builder、Dore/Kawahara 模板和地图联动。

BSM 事件侧已实现：

- raw/normalized event table；
- event-through-time；
- source-destination edge/node 表；
- Circle network；
- Map network editor；
- 曲线与标签拖动、投影、布局保存和 PNG/CSV/GeoJSON 导出。

### 1.9 大树与 Lineage Range Dynamics

1534-tip 数据暴露了 ETE3 vendor 版在矩形树、环形树、tip 标签、节点位置和窗口尺寸上的问题。近期已增加：

- 大树矩形/环形渲染适配；
- tip 状态与标签位置修正；
- 多个对话框可调整大小/最大化；
- 代表性窗口行为检查；
- 专门的大树渲染检查工具。

`Lineage Range Dynamics` prototype 已实现 node interpolation、BGB endpoints、BSM summary/single-map、树图与地图联动。它与旧任务书删除的“全树枝-时间多状态概率曲线场”存在边界冲突，且当前产品价值与科学语义不足以支持公开发布。

本任务书重新定义：

- 原先声称“任意枝任意时间的祖先分布后验概率场”的功能仍为 Removed；
- 当前 `Lineage Range Dynamics` 状态为 **Sealed prototype**，结果窗口不显示入口，主窗口不向其注入运行上下文；
- node interpolation 只能解释为节点状态之间的展示插值，不能解释为真实枝内历史；
- BSM 模式必须明确区分 summary 与 single stochastic map；
- 底层代码和专项检查暂时保留，后续只有完成独立产品重设计后才能重新启用；开发记录见 `docs/lineage_range_dynamics/README.md`。

## 2. 产品边界与结果语义

### 2.1 产品定位

RASP5 是一个面向历史生物地理与祖先性状分析的可审计桌面工作台。它连接树、树集合、范围/性状矩阵、occurrence、GeoJSON、成熟外部引擎、标准化祖先结果、BSM 事件和可复现导出。

RASP5 不是：

- 第三方算法的 Python 重写集合；
- 所有旧版 bug 的逐字符复制；
- 当前 Rust BioGeoBEARS 重写的 GUI 外壳；
- 已经提出新祖先分布统计模型的软件；
- 为了“功能多”而持续增加顶层菜单的工具箱。

### 2.2 三条必须分离的结果语义

1. **祖先节点状态**：DIVA、DEC、BioGeoBEARS、BayArea、BBM 等输出的节点范围或概率。
2. **启发式事件摘要**：普通结果窗口 Information/Time 根据节点最优状态推断的 dispersal/vicariance/extinction，不是后验随机历史。
3. **BSM 随机映射事件**：来自 BioGeoBEARS stochastic maps 的 `ana.events` / `clado.events`，可用于事件频率、时间和迁移网络统计。

`Lineage Range Dynamics` 还必须额外区分：

- node interpolation：展示插值；
- BGB endpoints：模型端点结果之间的展示；
- BSM summary：多条随机历史的汇总；
- single BSM map：一条抽样历史，不代表确定历史。

### 2.3 旧版兼容原则

| 类型 | 处理原则 |
| --- | --- |
| 算法逻辑 | 优先查旧源码、旧输入和原始输出，确认后对齐 |
| 新引擎语义 | 旧功能不能原生映射时按新引擎重新设计，不伪造等价性 |
| UI 工作流 | 在不误导算法含义时保持旧用户熟悉的操作方式 |
| 显示约定 | 可以兼容，但必须与实际状态空间分开 |
| 已确认旧 bug | 不复刻，记录差异和理由 |

## 3. 功能成熟度定义

| 等级 | 含义 | 发布处理 |
| --- | --- | --- |
| Stable candidate | 主链完整，有真实运行和基础回归，等待 release gate | 默认显示 |
| Beta | 功能可用，但数值、大数据或交互验证尚不足 | 显示 Beta/限制 |
| Experimental | 可探索，科学语义或稳定性尚未收口 | Advanced 中显示 |
| Deferred | 有价值，但不进入首个正式版本 | 不阻塞发布 |
| Removed | 已确认价值不足、语义不成立或设计有害 | 不保留用户入口 |

## 4. 当前真实实现状态

### 4.1 祖先分布与模型分析

| 模块 | 状态 | 已完成 | 当前缺口 |
| --- | --- | --- | --- |
| DIVA | Stable candidate | 单树配置、range constraints、Fossil、结果树、Information/Time、大树编码 | 当前工作树金标准和 1534-tip 回归 |
| S-DIVA | Stable candidate | 修改版 DIVA config/proc、随机抽树、Final tree/Fossil、Threads、Save/Load、有效/失败/unmatched 及 topology/node identity 审计 | 后续数据集扩展回归 |
| DEC | Stable candidate | 修复版 lagrange-ng、maxareas、period rules、dispersal constraints、Fossil/MRCA、root scaling | 固定 d/e、period d/e 与大树回归纳入门禁 |
| S-DEC | Stable candidate | 外层按树并行、OpenBLAS 单线程、clade 合并、配置持久化、unmatched clade 报告 | 全 1001 树资源回归 |
| BioGeoBEARS | Stable candidate | 六模型、DEC*、null range、+J 启动、time-stratified、cores | 多数据集数值基准和失败模型诊断 |
| S-BioGeoBEARS | Stable candidate | batch runner、旧式 clade 合并、进度、参数配置和统一树计数 | 100/1000 树资源测试和中断行为 |
| BGB model test | Stable candidate | 六模型 batch、AIC/AICc/weights、LRT/teststable | 独立 R 结果金标准 |
| BSM generation/load | Stable candidate | 独立生成、载入和 raw event 解析 | 固定 1000-map benchmark 缺失 |
| BayArea DISTANCE NORM | Beta/release candidate | 修复版引擎、坐标、MCMC、Tracer、seed、多链、原始结果 | 独立链收敛和长链文档 |
| BayArea INDEPENDENCE | Compatibility only | 保留入口和明确警告 | 不作为推荐分析，不阻塞发布 |
| BBM | Beta | MrBayes runner、节点选择、OG0、进度/取消、树图 | 旧 BBM 案例系统对照 |

### 4.2 性状重建

| 模块 | 状态 | 已完成 | 当前缺口 |
| --- | --- | --- | --- |
| BayesTraits MultiState | Beta | V5 ML/MCMC、节点选择、配置和解析 | ML 输出与节点可视化边界文档 |
| BayesTraits Continuous A/B | Beta | MCMC unknown-value、none/log/log10、back-transform、连续树图 | 收敛诊断、大树限制、单树金标准 |
| BayesTraits 其他模式 | Runner complete/result-specific | IC、Correlation、Fat Tail 等入口 | 独立结果页，不伪装成节点重建 |
| phytools fastAnc/CI/anc.Bayes | Stable candidate/mixed | 连续性状估计和树图 | R 原生输出对照、缺失值规则 |
| phytools anc.ML BM/OU/EB | Experimental | 入口和失败诊断 | 数值可靠性验证，不作为默认 |
| ape ace ER/SYM/ARD | Stable candidate | 离散性状重建 | 多状态、缺失值和大树回归 |
| S-phytools/S-ape | Beta | 树集合快速性状入口 | 聚合语义、节点 identity、性能上限 |
| Publication A/B/C figure | Experimental presentation | contMap-like、group/regime、图形导出 | 与正式估计和论文算法解耦 |
| DTT | Experimental analysis | 单/多树时间切片基础 | 产品边界和统计定义尚未冻结 |

参考源码中存在 ChromEvol 相关代码，但当前 Python 版未接入，且用户实际使用的旧版菜单未确认包含该功能。ChromEvol 归入 Deferred scope decision，不作为 RASP5 v5.0 发布阻塞项。

### 4.3 空间数据中间层

| 模块 | 状态 | 已完成 | 当前缺口 |
| --- | --- | --- | --- |
| Spatial Data Manager | Stable candidate | occurrence、GeoJSON、QA、预览/Show all、进度/取消、矩阵编码 | 用户教程和更多格式回归 |
| Point-in-polygon | Stable candidate | bbox 预筛选、hole、多面基础、149k 测试 | 跨 180 度和复杂边界 fixture |
| Taxon matching | Stable candidate | exact/normalized/prefix/ambiguous/unmatched audit | synonym 服务 Deferred |
| Matrix import | Stable candidate | CSV/XLSX、混合离散/连续列、用户选列语义 | 大文件内存和错误信息 |
| Region GeoJSON Builder | Beta | GUI、template/custom/advanced、base layer、mapping/rules | 通用规则编辑和来源引用 |
| Spatial map view | Beta | occurrence/area/audit 联动和 polygon 高亮 | 大数据交互与视觉一致性 |

### 4.4 BSM 事件、网络和时空展示

| 模块 | 状态 | 已完成 | 当前缺口 |
| --- | --- | --- | --- |
| Event table | Stable candidate | raw/normalized events、筛选、Summary/Time、CSV | schema 固化和 1000-map 回归 |
| Event through time | Beta | 时间分箱、方向、事件类型、CSV | rate 分母与多 map 区间定义 |
| Source-destination network | Beta | edge/node、mean per map、richness、阈值、导出 | Dore 原脚本逐边对照 |
| Circle network | Beta | 无空间数据布局、过滤、线宽 | 字体、图例和布局稳定性 |
| Map network editor | Beta | GeoJSON、曲率/标签拖动、投影、布局、导出 | 自动交互回归和 layout schema |
| Lineage Range Dynamics | Sealed prototype | node interpolation、BGB endpoints、BSM summary/single map、树图/地图联动代码保留 | 不进入 M1；重新定义科学问题后再决定是否恢复 |
| Time-slice occupancy | Deferred | 尚未形成可信产品 | 不阻塞 v5.0 |
| Posterior/tree-set BSM aggregation | Deferred | 未实现 | 发布后研究任务 |

### 4.5 大树与结果视图

| 模块 | 状态 | 已完成 | 当前缺口 |
| --- | --- | --- | --- |
| ETE3 vendor 大树渲染 | Beta | 环形/矩形专项适配、tip/节点位置改进 | 1534-tip 固定结果 fixture 和导出回归 |
| 结果窗口布局 | Beta/stable candidate | 多窗口可调整大小、最大化、标题整理 | 全菜单手工验收 |
| Information/Time | Stable candidate | 启发式事件、曲线、反转时间轴、显示开关 | 当前方法组合回归和语义文档 |
| Node identity/list | Stable candidate with risk | 统一结果 schema 和节点列表 | 跨 S 方法、大树、不同 topology 审计 |

### 4.6 发布工程

| 模块 | 当前状态 | 说明 |
| --- | --- | --- |
| Git checkpoint | Complete | 当前源码、数据迁移、文档和验证证据已固定为 `rasp5-m0.5-20260827` |
| Engine bundle | Internal structure complete / audit passed | 15 个关键文件和 public payload audit 已通过 |
| README | Not release-ready | 根 README 仍只有项目测试占位文本 |
| 项目许可证 | Missing | 第三方引擎许可已整理，自有代码许可证未确定 |
| CITATION/CHANGELOG | Missing | 尚未形成正式版本身份 |
| CI | Missing | 没有 GitHub Actions 或等价门禁 |
| Installer | Not started | 尚无最终 Windows 安装制品 |

## 5. 2026-08-27 审计证据

| 检查 | 结果 | 解释 |
| --- | --- | --- |
| 第一方 Python 3.6 compile | 169 files passed | 排除 bundled vendor ETE3 后通过 |
| Phase 1 spatial checks | Passed | occurrence、polygon hole、metadata、region builder、taxon matching、worker/UI |
| 149k occurrence benchmark | Passed | 149,541 行；导入约 24.7 s，编码约 53.1 s；audit 行数一致 |
| Window behavior checks | 19 representative dialogs passed | 可调整大小/最大化主链基本可用 |
| Temporal range checks | Passed | branch、endpoint 和 BSM history 基础检查通过 |
| Large-tree rendering check | Passed | 固定 1534-tip Dore fixture 和确定性 synthetic result；环形/矩形及结果上下文通过 |
| Phase 2 spatial checks | Passed | 独立 two-map BSM fixture 覆盖 parser、event/time、Spatial Project、network statistics/export、Map/Circle 一致性和布局刷新回归 |
| Public engine bundle audit | Passed | 15 个关键文件、14,421 个 payload 文件和 188 个 R package inventory rows 通过 |
| Phase 0 smoke | Passed on current worktree | 2026-08-27 实算 DIVA/S-DIVA、DEC/S-DEC、BGB 六模型与 BSM 通过 |
| WP2 native parameter contract | Passed | fixed d/e、period d/e、maxareas、matrix/exclude A/B、BayArea prior/seed/initial rates/multi-chain pooling 均经原生引擎输出验证 |
| Psychotria numerical gold | Passed | DIVA/S-DIVA、DEC/S-DEC、BGB/S-BGB 和六模型检测的 18 节点完整状态/概率及模型统计已冻结并通过 compare |
| S-series accounting | Passed | 三种 S 方法统一报告 input/effective/failed/zero-contribution/unmatched clade，异常树、未知状态和部分 DIVA 失败均有回归 |
| Dore-derived 1000-map benchmark | RASP adjacency-path specification validated | 固定作者 commit、输入 hash、DEC+J 参数、1000 maps/seed/`maxnum_maps_to_try` 和运行工具；作者手工 allowed-state 路径与正式数值输出仍待 WP5 |

结论：WP1 已让测试事实与当前工作树重新同步。完整命令、版本、输入、参数和耗时见 `docs/WP1_测试事实报告_20260827.md`。下一风险中心是数值金标准和参数传播，而不是继续补 smoke test 名称。

## 6. 已删除、实验隔离与延期

### 6.1 Removed

1. **Node Range Map / Range Map**：粗颗粒离散 range 对 polygon 着色不能形成真实空间概率热图，信息增量不足。
2. **把节点状态插值宣称为枝内后验概率场**：节点端点之间的平滑插值没有生成真实历史概率，不能作为算法结果。

### 6.2 Sealed prototype decision

`Lineage Range Dynamics` 已完成当前阶段产品决策：

- 删除结果窗口公开入口，停止正常结果流程的数据上下文注入；
- 保留内部 prototype、语义警告和专项检查，不进入 v5.0/M1；
- 后续若恢复，必须先明确单一研究问题、四种 source mode 的边界和大树交互方案。

### 6.3 Deferred

- posterior/tree-set BSM uncertainty；
- focal source/sink 正式统计产品；
- time-slice occupancy；
- paleomap/GPlates；
- raster/environment overlay；
- S-BayesTraits 贝叶斯树集合；
- ChromEvol scope；
- 大型标准生物地理区数据库；
- Rust BioGeoBEARS 重写；
- 新祖先分布模型。

## 7. 工程实施原则

本项目不采用“为了防止一切可能错误”而层层加 fallback 的方式。只在真实边界和已观察回归处做检查。

### 7.1 必须坚持

1. 每个参数只有一个权威来源：GUI 生成 config，service/runner 不再保留重复隐藏入口参数。
2. 外部引擎失败时保留真实 command、config、stdout/stderr 和 run directory。
3. 不支持的模型语义直接禁用或明确标记，不做静默近似。
4. parser 只接受已知输出契约；格式变化应明确失败，不凭猜测拼出结果。
5. 每个真实 bug 增加一个能复现原问题的最小回归测试。
6. 测试使用固定 fixture，不依赖用户当前 `runs` 目录。

### 7.2 明确避免

- blanket `try/except` 后返回空结果；
- 多层自动重试和自动更换模型；
- “旧版失败就新引擎、再失败就 Python 近似”的兼容链；
- service 和 GUI 同时维护相同默认值；
- 为理论上所有异常组合编写大量不可触达分支；
- 用兜底归一化强行让结果接近旧版百分比。

最小验证标准：每个稳定引擎一套参数传播测试、一个固定数值金标准、一个真实工作流测试；每个有状态 GUI 一套针对已发生刷新回归的交互测试。

## 8. 当前工作包

### WP0：范围冻结与仓库基线

目标：把当前开发现场变成可回溯 checkpoint。

任务：

1. 审核当前 `git status` 中全部 tracked/untracked changes，并按源码、测试数据、文档、引擎和生成物分类；
2. 排除论文代码 ZIP、Excel 临时文件、大型原始数据和普通 `runs` 产物；
3. 提交窗口、大树、Lineage Range Dynamics 和 renderer 当前状态；
4. 记录源码 commit、Python 环境和 engine bundle 对应关系；
5. 冻结 Stable/Beta/Experimental/Deferred/Removed 清单；
6. 已决定 Lineage Range Dynamics 的 M1 产品状态：Sealed prototype，不进入公开菜单和正常结果数据流。

退出标准：

- `git status` 干净；
- 新 clone 可恢复当前源码；
- 当前功能清单和菜单一致；
- 没有用户可见死入口或任务书/代码冲突。

### WP1：测试事实修复

目标：先保证“测试通过”确实覆盖它声称覆盖的功能。

状态：**Complete（2026-08-27）**。证据见 `docs/WP1_测试事实报告_20260827.md`。

任务：

1. 重写 `run_phase2_spatial_checks.py`，不再转发 Phase 1；
2. 已将 large-tree check 改为仓库内固定 1534-tip fixture 和确定性 synthetic result；
3. 已更新 engine bundle manifest 中 `bgb_runner.R` 的 hash/size，并通过 public audit；
4. 已将 Dore 衍生的 RASP adjacency-path 1000-map BSM 固定为带作者 commit、输入 hash、参数和运行清单的可重建资产；作者 allowed-state 路径等价性、正式结果重跑和论文统计对照归入 WP5；
5. 在当前工作树重新运行 Phase 0、Phase 1、Phase 2、窗口和大树检查；
6. 测试报告记录版本、引擎 hash、参数、数据和耗时。

退出标准：

- Phase 0/1/2 各自覆盖不同契约；
- 测试不读取可变 debug run；
- engine bundle audit 通过；
- 当前 commit 有一份完整 smoke report。

### WP2：核心方法数值可信度

目标：回答“哪个参数通过哪条路径进入哪个引擎，并产生什么结果”。

状态：**In progress（2026-08-28）**。参数传播、Psychotria 数值金标准、S 系列计数和 clade/node identity 已完成；下一阻断项是 BBM 对照。证据见 `docs/WP2_参数传播与数值基准_20260827.md`、`docs/wp2_parameter_contract_latest.md`、`docs/psychotria_gold_latest.md`、`docs/s_series_accounting_latest.md` 和 `docs/clade_node_identity_latest.md`。

已完成：

- lagrange-ng fixed d/e、maxareas、period d/e 继承/覆盖进入原生 JSON/stdout；
- period matrix 与 exclude rule 通过 baseline/matrix-only/exclude-only/combined 四路原生 likelihood A/B 对照；
- 修复纯时间分层速率在默认 matrix/no area rule 时不生成 period 定义的问题；
- BayArea distancePowerPrior、seed、Guess Initial Rates 和双链执行进入原生输出；
- 双链合并后的用户概率已核对为各链原始状态计数之和除以合并样本数；
- 输入、engine hash、Python 版本、平台、commit 和运行目录写入机器可读报告。
- Psychotria 的 DIVA/S-DIVA、DEC/S-DEC、BGB/S-BGB 和六模型检测已建立完整数值 baseline；
- S-DIVA、S-DEC、S-BGB 已统一方法级和节点级 input/effective/failed/unmatched 计数、失败明细、日志与 CSV。
- clade identity 已统一为排序后的后代 taxon 集合，显示节点号统一为 `tip 数 + postorder 内部节点序号`；Psychotria 全 1001 树和 Dore 1534-tip 树均已审计。

尚未完成：BBM 旧案例对照。

任务：

1. 为 Stable candidate 建立 `GUI -> config -> raw input -> raw output -> parsed result` 追踪表；
2. [完成] 固化 Psychotria 的 DIVA/S-DIVA、DEC/S-DEC、BGB/S-BGB/model test 金标准；
3. [完成] 将 lagrange-ng fixed d/e、period d/e、maxareas、period rules 纳入自动测试；
4. [完成] 将 BayArea distancePowerPrior、seed、initial rates、multi-chain 纳入测试；
5. [完成] 统一 S 系列报告 `input/effective/failed/unmatched tree count`；
6. [完成] 统一 clade/node identity 审计，覆盖 topology 差异、缺失 clade、缺失/重复 taxon、polytomy、rerooting 和 unary ambiguity；
7. BBM 与旧版案例完成至少一套系统对照。

退出标准：

- 每个 Stable candidate 至少一个原生引擎金标准；
- 不存在可编辑但未进入计算的参数；
- 数值差异有可解释来源和容差；
- S 方法分母和丢弃原因可见。

### WP3：大树与结果视图收口

目标：让 1534-tip 级树能够打开、交互和导出，而不是只在小树上正确。

任务：

1. 固定矩形树、环形树、tip 状态、标签和节点位置的渲染规则；
2. 大树默认隐藏高成本/低信息元素，并允许用户按需启用；
3. 测试 DIVA、DEC、BGB、连续性状和 Lineage Range Dynamics renderer；
4. 验证放大、缩小、适应窗口、环形切换、选择节点和导出 PNG/SVG/PDF；
5. 将窗口尺寸行为覆盖到实际菜单入口，而非只构造 dialog；
6. 不再直接在 ETE3 vendor 内叠加无测试补丁；所有 vendor 修改必须有专项回归。

退出标准：

- 1534-tip 数据打开和切换布局不崩溃；
- tip 状态不飞到文字后方，矩形树不再压成不可读黑线；
- 交互状态刷新后不丢失；
- 导出与屏幕布局语义一致。

### WP4：性状模块收口

目标：每个模型只展示它真正计算的结果。

任务：

1. BayesTraits 配置按模型与 ML/MCMC 动态显示参数；
2. 只有支持节点估计的路径进入祖先状态树图；
3. 固化 Model A/B、fastAnc、anc.Bayes、ace 结果 schema；
4. log/log10、back-transform、CI 和显示尺度写入元数据；
5. anc.ML BM/OU/EB 保持 Experimental，不做静默 fallback；
6. Publication A/B/C 和 DTT 保持 Experimental export，与估计服务分离；
7. 用 Primate 固定数据完成单树和树集合回归。

退出标准：

- 用户不会把模型参数误认为节点状态；
- 混合连续/离散矩阵按用户选择列工作；
- 每张连续性状图能追溯到分析尺度和估计来源。

### WP5：空间与 BSM 事件链收口

目标：从 occurrence 到 range matrix，再到 BSM 事件网络形成可审计闭环。

任务：

1. 固化 Spatial Project、Region Builder、event 和 network layout schema；
2. 保留 149k occurrence 全量 benchmark，并补跨 180 度/复杂 polygon fixture；
3. 用 Dore 1000-map 对照原论文/R 脚本的 edge count 和标准化；
4. 明确 count、mean per map、per-richness 和阈值；
5. 测试 Event Table、Summary、Time、CSV 行数同步；
6. 测试 Circle/Map 参数隔离、曲率/标签刷新和布局恢复；
7. 地图层只影响展示，不得改变统计 edge table。

退出标准：

- 同一空间输入和规则重复编码一致；
- Dore 关键网络边与参考脚本一致；
- 无空间数据时 Circle network 独立工作；
- 导出文件能完整重建网络图。

### WP6：发布工程

目标：让项目从开发机工程变成可安装软件。

任务：

1. 重写 README；
2. 添加 RASP5 自有代码许可证；
3. 添加 `CITATION.cff`、CHANGELOG、版本号和第三方引擎说明；
4. 添加 CI，至少执行静态和纯 Python 测试；
5. 构建 public engine bundle 和 Windows installer；
6. clean Windows 测试安装、运行、卸载、中文/空格路径；
7. 提供 Psychotria、Primate、Dore 三套端到端教程；
8. 决定 Python 3.6 是首版冻结运行时还是启动迁移。

退出标准：

- 新用户不查看源码即可完成分析；
- 安装包不依赖开发机绝对路径；
- 结果记录应用和引擎版本/hash；
- 发布资产可由 manifest 重建。

### WP7：论文证据与外部验证

建议主线：

> RASP5 provides an auditable desktop workflow from occurrence and range data to ancestral reconstruction and stochastic biogeographic event synthesis.

最低证据包：

1. 架构与数据流图；
2. 旧版/原生引擎/RASP5 数值一致性；
3. tree size、area count、tree-set、occurrence 和 BSM map 性能；
4. Dore 或同等级案例的端到端事件网络；
5. 参数映射、失败模式、版本和环境补充材料；
6. 2–3 名未参与开发用户按教程复现。

首个现实目标仍是 MBE Resource 或 Methods in Ecology and Evolution 类型软件/方法文章。Nature Communications 需要额外的重要生物学故事；Nature Methods 需要新的方法学核心和系统仿真比较，均不应通过继续堆 GUI 功能来追求。

## 9. 发布里程碑

### M0.5：Integration and evidence checkpoint（当前）

已经具备：

- 旧版核心祖先分布方法主链；
- 性状重建板块；
- occurrence 到 range matrix 空间中间层；
- BSM event/network 工作流；
- 修复版 lagrange-ng 和 BayArea；
- 大树和窗口专项改进；
- 独立引擎包结构。

进入 M1 前仍需完成：

- WP1 已完成：Phase 0/1/2、窗口、大树、temporal 和 engine audit 均有当前证据；
- WP2 尚未完成：参数传播、Psychotria 数值金标准、S 系列有效树及 node/clade identity 已固化；BBM 对照是进入 M1 前剩余的核心门槛；
- README、根许可证、CI、installer 仍缺失，但属于 M2/WP6 发布门槛。

### M1：Feature-complete Beta

必须完成 WP0、WP1、WP2 核心项，并冻结 WP3/4/5 范围。

进入标准：

- Stable/Beta/Experimental 清单与菜单一致；
- 所有 Stable 方法至少一个真实引擎金标准；
- Phase 0/1/2 和大树测试真实覆盖对应功能；
- 没有参数静默失效、数据损坏或已知结果错配；
- Lineage Range Dynamics 保持移出菜单且不参与正常结果数据流。

### M2：Release Candidate

必须完成 WP3–WP6。

进入标准：

- clean clone + engine bundle + installer 可运行；
- 数值、GUI、大数据和 clean release gate 通过；
- README、license、CITATION、CHANGELOG 完成；
- 至少一次外部用户测试。

### M3：Manuscript-ready

必须完成 WP7。

进入标准：

- benchmark 和论文图可由固定 release 重建；
- 主要主张不依赖本机临时文件；
- 第三方可复现核心案例；
- 清楚区分 RASP5 贡献与第三方算法。

### M4：Advanced research

M3 后再推进 posterior BSM、paleomap、Rust 核心、新模型或新的生物学故事。

## 10. 固定验证数据矩阵

| 数据 | 主要用途 | 必须检查 |
| --- | --- | --- |
| Psychotria | 旧版兼容和核心方法 | DIVA/S-DIVA、DEC/S-DEC、BGB/S-BGB、model test、BSM |
| Primate | 离散/连续性状 | BayesTraits、ape、phytools、log/back-transform、缺失值 |
| Dore Ponerinae | 1534-tip、7 区、149k occurrence、BGB/BSM/network | 大树、空间编码、1000-map 网络和论文对照 |
| Synthetic small | 单元测试和错误注入 | null range、maxareas、period、polytomy、unmatched、hole/multipolygon |
| Additional public sets | 泛化能力 | 不同 tree size、area count、类群和数据质量 |

预览子集只用于 UI 开发，不用于科学或性能结论。

## 11. 当前风险登记

| 风险 | 当前事实 | 处理 |
| --- | --- | --- |
| checkpoint 后续漂移 | M0.5 已固定，后续工作会继续产生新提交 | 每个工作包独立提交并保持 `rasp5-m0.5-20260827` 可回退 |
| 参数表面接入但计算未使用 | lagrange-ng/BayArea 已真实发生 | 每个稳定引擎最小参数传播测试 |
| 测试依赖可变运行目录 | large-tree check 已发生 | 固定 fixture，不读用户 debug run |
| Phase 名称制造假覆盖 | 已修复：Phase 2 使用独立 deterministic BSM contract | 后续只按真实契约扩展，不再转发其他 Phase |
| BSM 完整基准丢失 | 已有 Dore 衍生的 adjacency-path 可重建规范，正式 1000-map 输出尚未重跑 | WP5 先核对 allowed-state 路径，再运行并固化数值/论文网络统计证据 |
| 节点/clade identity | S 方法和大树仍有跨方法风险 | 统一 identity 审计和有效树报告 |
| GUI 刷新破坏状态 | BSM editor 曾多次发生 | model state 与绘图分离，针对真实回归测试 |
| Experimental 范围漂移 | Trait figure、DTT、Lineage Dynamics | 决策 gate，不再继续扩入口 |
| Python 3.6 生态老化 | 当前运行时为 3.6.13 | v5.0 冻结或迁移二选一，不同时推进 |
| 发布身份缺失 | README/license/CI/installer 不完整 | WP6 |

## 12. 立即执行顺序

1. **已完成：建立 M0.5 checkpoint**，标签为 `rasp5-m0.5-20260827`。
2. **持续约束：保持 Lineage Range Dynamics 封锁**，不纳入 M1。
3. **已完成：修复测试事实**，包括独立 Phase 2、large-tree fixture、engine manifest 和 Dore rebuild specification。
4. **已完成：重跑当前版本证据**，报告见 `docs/WP1_测试事实报告_20260827.md`。
5. **当前执行：核心数值金标准**，从 Psychotria 和原生引擎参数追踪开始。
6. **收口性状、空间和 BSM 网络**：只修语义、回归和导出，不再扩方法。
7. **发布基础**：README、license、CITATION、CI、installer、clean Windows。
8. **外部测试和论文 benchmark**。

在第 1–6 步完成前，不新增 paleomap、raster、posterior BSM、ChromEvol、S-BayesTraits 或新顶层地图模块。

## 13. RASP5 v5.0 完成定义

RASP5 v5.0 可以宣布完成，必须同时满足：

1. 所有 Stable 方法能从 GUI 运行、取消、记录参数并打开结果；
2. 每个可编辑参数有真实计算映射或明确说明；
3. 核心方法有原生引擎/旧版金标准和声明容差；
4. S 方法报告输入树、有效树、失败树和 unmatched clade；
5. Spatial Data 能从真实 occurrence/GeoJSON 产生可审计矩阵；
6. BSM 能产生可复现的事件表、时间汇总和迁移网络；
7. Trait Reconstruction 只展示模型实际支持的结果；
8. 1534-tip 树可打开、交互和导出；
9. Phase 0/1/2、GUI、大数据、engine bundle 和 clean release gate 通过；
10. README、教程、license、CITATION、CHANGELOG 和第三方许可完整；
11. 安装包可在干净 Windows 使用；
12. 论文案例可从固定 release 重建。

## 14. 任务书维护规则

1. 本文档是当前权威路线；旧任务书只保留历史决策。
2. 新功能必须先说明研究问题、数据来源、结果语义、成熟度和所属里程碑。
3. 修复 bug 后更新对应测试或验证记录，不只保留聊天说明。
4. “有 UI”“有 parser”“跑过一次”均不等于 Stable。
5. Removed 功能不能换名字重新加入；若重新设计，必须明确与旧方案的语义差异。
6. 不为理论上的所有异常增加防御层，只针对真实边界和已观察回归。
7. 不用 fallback 或后处理强行逼近旧版结果。
8. 论文目标不改变正确性、可复现性和诚实边界的优先级。
